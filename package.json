{
  "scripts": {
    "check": "npm run check:backend && npm run check:frontend && npm run check:ml",
    "check:backend": "cd churn-backend && npm run lint && npm run build && npm test",
    "check:frontend": "cd churn-frontend && npm run lint && npm run build && npm test",
"check:ml": "cd ml-api && python -m venv .venv && .venv\\Scripts\\python.exe -m pip install -r requirements.txt && .venv\\Scripts\\python.exe -m pytest -v"
    }
}
